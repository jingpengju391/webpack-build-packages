module.exports = {
    // type: "module",
    preset: 'ts-jest',
    // extensionsToTreatAsEsm: ['.ts'],
    // transform: {
    //     '^.+\\.jsx?$': 'babel-jest',
    //     '^.+\\.m?js$': 'babel-jest',
    //     '^.+\\.ts?$': 'ts-jest',
    //     '^.+\\.tsx?$': 'ts-jest',
    // },
    // moduleNameMapper: { // 如果您的项目中使用了一些不支持 ECMAScript 模块的第三方库，可以使用 moduleNameMapper 配置选项来将它们替换为模拟对象
    //     '^lodash-es$': 'lodash',
    // },
}

